@model IEnumerable<SchulungQotd.Shared.Models.AuthorViewModel>

<table class="table table-hover">
    <thead>
        <tr>
            <th>Photo</th>
            <th>Name</th>
            <th>Beschreibung</th>
            <th>Geburtsdatum</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var author in Model)
        {
            <tr>
                <td>
                    <image-data-uri
                        image="@author.Photo"
                        image-mime-type="@author.PhotoMimeType"
                        width="120">
                    </image-data-uri>
                </td>
                <td>@author.Name</td>
                <td>@author.Description</td>
                <td>@author.BirthDate</td>
                <td>
                    <!-- Aktionen -->
                </td>
            </tr>
        }
    </tbody>
</table>