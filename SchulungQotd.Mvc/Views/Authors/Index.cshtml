@model IEnumerable<SchulungQotd.Shared.Models.AuthorViewModel>

<table class="table table-hover">
    <thead>
        <tr>
            <th>Photo</th>
            <th>Name</th>
            <th>Beschreibung</th>
            <th>Geburtsdatum</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var author in Model)
        {
            <tr>
                <td>
                    <image-data-uri
                        image="@author.Photo"
                        image-mime-type="@author.PhotoMimeType"
                        width="120">
                    </image-data-uri>
                </td>
                <td>@author.Name</td>
                <td>@author.Description</td>
                <td>@author.BirthDate</td>
                <td>
                    <!-- Aktionen -->
                    <a asp-action="Details" asp-route-id="@author.Id">Details</a>
                    <a asp-action="Delete" asp-route-id="@author.Id"><i class="bi bi-trash"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>